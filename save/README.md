# Local source data

Place operator-managed Anoka County CSV snapshots in this directory when using
the local analysis and ingestion scripts. CSV files are ignored by Git and are
not published in the public repository.

The comparable workflow expects parcel IDs to remain text values with a
12-character width, including leading zeroes. The required property and
tax-year fields are documented in the repository's functionality requirements.
