# Geometry and coverage

Parcel geometry is keyed by `Parcel_ID`/county `PIN`. Property overlays remain
limited to imported CSV parcel IDs. The basemap tile gate is bounded to the
Anoka County envelope represented by the non-empty CSV snapshot until the
county geometry import populates an exact parcel-derived tile index.
