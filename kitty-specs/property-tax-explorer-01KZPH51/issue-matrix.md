# Issue matrix — property-tax-explorer-01KZPH51

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #11 | Add import publication and self-hosted operations | fixed | `cd375be` (`operations/`, `deploy/`, deployment docs) |
| #10 | Build the county-wide map explorer | fixed | `cd375be` (`county-ui/`, MapLibre/OpenStreetMap shell) |
| #12 | Add tests, benchmarks, accessibility, and security validation | fixed | `cd375be` (`zig build test`, validation/security docs) |
| #13 | Add the independent statistical model | fixed | `cd375be` (`model/`, statistical-model docs) |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
