# Contract artifacts

The public HTTP contract is currently documented in `docs/api.md` and exposed
by the read-only routes in `app/app.zig`. This directory is reserved for
machine-readable OpenAPI or schema artifacts as the API stabilizes.
